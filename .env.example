# Copy to .env (gitignored) and fill in your token.
# Any GitHub personal access token works; no scopes are needed to read
# public data. Create one at https://github.com/settings/tokens
GITHUB_TOKEN=
